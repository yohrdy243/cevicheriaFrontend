import { LineaPedidoBebida } from './linea-pedido-bebida';

describe('LineaPedidoBebida', () => {
  it('should create an instance', () => {
    expect(new LineaPedidoBebida()).toBeTruthy();
  });
});
