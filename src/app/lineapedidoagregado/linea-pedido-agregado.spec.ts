import { LineaPedidoAgregado } from './linea-pedido-agregado';

describe('LineaPedidoAgregado', () => {
  it('should create an instance', () => {
    expect(new LineaPedidoAgregado()).toBeTruthy();
  });
});
